version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: hello-node:latest
    container_name: hello-node-prod
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000